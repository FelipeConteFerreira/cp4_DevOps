# Copie este arquivo para ".env" (mesma pasta do docker-compose.yml) e ajuste
# os valores. O arquivo ".env" real NUNCA deve ser commitado no Git
# (ja esta listado no .gitignore) - e assim que evitamos expor credenciais
# no codigo fonte / repositorio.

DB_USER=dimdim
DB_PASSWORD=SanSao123!
